{
  "name": "trivia-battle",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "install:all": "npm install --include=dev && npm install --include=dev --prefix client && npm install --prefix server",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "NODE_ENV=production npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
